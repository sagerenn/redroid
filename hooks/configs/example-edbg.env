# Example eDBG non-interactive breakpoint session knobs.
# eDBG is ptrace-free (eBPF). Run as root on device, ARM64 only.
#
#   redroid-hook edbg --package com.example.app --lib libnative-lib.so --break 0x1234
#
HOOK_PACKAGE=com.example.app
HOOK_LIB=libnative-lib.so
HOOK_BREAK=0x0
